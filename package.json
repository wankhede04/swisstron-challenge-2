{
  "name": "hardhat-project",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^2.0.2",
    "hardhat": "^2.18.0"
  },
  "dependencies": {
    "dotenv": "^16.3.1"
  },
  "scripts": {
    "compile": "hardhat compile",
    "clean": "hardhat clean",
    "test": "hardhat test",
    "deploy": "hardhat run --network $NETWORK scripts/deploy.ts",
    "readValue": "hardhat run --network $NETWORK scripts/readValue.ts"
  }
}
